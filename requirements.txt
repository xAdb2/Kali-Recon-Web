# Runtime dependencies for the app/worker image (pinned ranges).
Django>=5.1,<5.2
celery>=5.4,<5.5
redis>=5.0,<6.0
psycopg[binary]>=3.2,<4.0
gunicorn>=23.0,<24.0
docker>=7.1,<8.0
whitenoise>=6.7,<7.0
